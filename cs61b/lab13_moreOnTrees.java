Trees
{
	for a stack{{
		pop method remove the first item of the collection and makes it the return of the function
		push method add the item to the first, void type
	}}
	for a queue{{
		enqueue method add new item to the behind of the queue,void type
		dequeue method remove the first item of the queue and return it
	}}

}
